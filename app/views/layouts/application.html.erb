<!DOCTYPE html>
<html>

<head>
<title>a2f Ohana Winter Reading Challenge</title>
<%= csrf_meta_tags %>
<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
<%= stylesheet_link_tag 'application', "https://fonts.googleapis.com/css?family=Cormorant+Garamond", media: 'all', 'data-turbolinks-track' => true %>

<%# <%= stylesheet_link_tag 'application','https://fonts.googleapis.com/css?family=Lato', media: 'all', 'data-turbolinks-track' => true %> 
<%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
<%= favicon_link_tag asset_path('a2f_logo_inverted.png'), :rel => 'icon', :type =>  'image/png' %>
</head>

<body>
<nav id="menu" class="navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
            </button>
            <%= link_to(root_path, class: "navbar-brand navbar-header") do %>
                <%= image_tag(asset_path("a2f_logo.png"), :size => "60x60") %>
                <h2>a2f Ohana</h2>
            <% end %>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li class="nav-link">
                    <%= link_to "Home", root_path %>
                </li>
                <li class="nav-link">
                    <%= link_to "Sharings", sharings_path %>
                </li>
                <li class="nav-link">
                    <%= link_to "Post", submit_post_path %>
                </li>
            </ul>
        </div>
    <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>

<div class="flash">
    <% flash.each do |message_type, message| %>
    <% if message_type == "success" %>
        <div class="alert alert-success alert-dismissible container" role="alert">
            <%= message %>
            <button class="close" data-dismiss="alert">&times;</button>
        </div>
    <% else %>
        <div class="alert alert-danger alert-dismissible container" role="alert">
            Error(s) with your submission: <%= message %>
            <button class="close" data-dismiss="alert">&times;</button>
        </div>
    <% end %>
    <% end %>
</div>
<%= yield %>

<script src="js/bootstrap.min.js"></script>
</body>

</html>
